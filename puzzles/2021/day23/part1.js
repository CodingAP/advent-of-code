module.exports = input => {
    let current = [
        ['b', 'd'],
        ['c', 'a'],
        ['b', 'd'],
        ['a', 'c']
    ];

    let reserved = ['', '', '', '', '', '', ''];

    let energyCosts = {
        a: 1,
        b: 10,
        c: 100,
        d: 1000
    }
    
    let energy = 0;

    let movePiece = (room, newRoom, isReserve = false) => {
        let piece = current[room].pop();
        if (current.)
    }
    return 0;
}
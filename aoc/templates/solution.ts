/**
 * <steve> return `puzzles/${steve.data.year}/day${steve.data.day.padStart(2, '0')}/solution.ts`; </steve>
 *
 * ~~ <steve> return steve.data.title; </steve> ~~
 * this is my solution for this advent of code puzzle
 *
 * by alex prosser
 * <steve> const today = new Date(); return `${today.getMonth() + 1}/${today.getDate()}/${today.getFullYear()}`; </steve>
 */

/**
 * the code of part 1 of the puzzle
 */
const part1 = (input: string) => {
    return 0;
};

/**
 * the code of part 2 of the puzzle
 */
const part2 = (input: string) => {
    return 0;
};

export { part1, part2 };
